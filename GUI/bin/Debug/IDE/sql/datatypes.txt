CHAR(^)-Chuỗi ký tự có độ rộng cố định
VARCHAR(^)-Chuỗi ký tự có độ rộng thay đổi
VARCHAR(^)-Chuỗi ký tự có độ rộng thay đổi
TEXT-Chuỗi ký tự có độ rộng thay đổi
NCHAR-Chuỗi Unicode có độ rộng cố định
NVARCHAR-Chuỗi Unicode có độ rộng thay đổi
NVARCHAR(^)-Chuỗi Unicode có độ rộng thay đổi
NTEXT-Chuỗi Unicode có độ rộng thay đổi
BINARY(^)-Chuỗi nhị phân chiều rộng cố định
VARBINARY-Chuỗi nhị phân có độ rộng thay đổi
VARBINARY(^)-Chuỗi nhị phân có độ rộng thay đổi
IMAGE-Chuỗi nhị phân có độ rộng thay đổi
BIT-Số nguyên có thể là 0, 1 hoặc NULL
TINYINT-Cho phép các số nguyên từ 0 đến 255
SMALLINT-Cho phép các số nguyên từ -32,768 đến 32,767
INT-Cho phép các số nguyên từ -2,147,483,648 đến 2,147,483,647
BIGINT-Cho phép các số nguyên từ -9,223,372,036,854,775,808 đến 9,223,372,036,854,775,807
DECIMAL(P,S)-Cố định độ chính xác và số tỷ lệ. Cho phép các số từ -10 ^ 38 +1 đến 10 ^ 38 –1. Tham số p cho biết tổng số chữ số tối đa có thể được lưu trữ (cả bên trái và bên phải của dấu thập phân). p phải là giá trị từ 1 đến 38. Mặc định là 18. Tham số s cho biết số chữ số tối đa được lưu ở bên phải của dấu thập phân. s phải là một giá trị từ 0 đến p. Giá trị mặc định là 0
NUMERIC(P,S)-Cố định độ chính xác và số tỷ lệ. Cho phép các số từ -10 ^ 38 +1 đến 10 ^ 38 –1. Tham số p cho biết tổng số chữ số tối đa có thể được lưu trữ (cả bên trái và bên phải của dấu thập phân). p phải là giá trị từ 1 đến 38. Mặc định là 18. Tham số s cho biết số chữ số tối đa được lưu ở bên phải của dấu thập phân. s phải là một giá trị từ 0 đến p. Giá trị mặc định là 0
SMALLMONEY-Dữ liệu tiền tệ từ -214.748.3648 đến 214.748.3647
MONEY-Dữ liệu tiền tệ từ -922.337.203.685.477.5808 đến 922.337.203.685.477.5807
FLOAT(^)-Dữ liệu số chính xác nổi từ -1,79E + 308 đến 1,79E + 308. Tham số n cho biết trường nên giữ 4 hay 8 byte. float (24) giữ trường 4 byte và float (53) giữ trường 8 byte. Giá trị mặc định của n là 53.
REAL-Dữ liệu số chính xác thả nổi từ -3,40E + 38 đến 3,40E + 38
DATETIME-Từ ngày 1 tháng 1 năm 1753 đến ngày 31 tháng 12 năm 9999 với độ chính xác 3,33 mili giây
DATETIME2-Từ ngày 1 tháng 1 năm 0001 đến ngày 31 tháng 12 năm 9999 với độ chính xác 100 nano giây
SMALLDATETIME-Từ ngày 1 tháng 1 năm 1900 đến ngày 6 tháng 6 năm 2079 với độ chính xác 1 phút
DATE-Chỉ lưu trữ một ngày. Từ ngày 1 tháng 1 năm 0001 đến ngày 31 tháng 12 năm 9999
TIME-Chỉ lưu trữ thời gian với độ chính xác 100 nano giây
DATETIMEOFFSET-Giống như datetime2 với việc bổ sung độ lệch múi giờ
TIMESTAMP-Lưu trữ một số duy nhất được cập nhật mỗi khi một hàng được tạo hoặc sửa đổi. Giá trị dấu thời gian dựa trên đồng hồ bên trong và không tương ứng với thời gian thực. Mỗi bảng chỉ có thể có một biến dấu thời gian
SQL_VARIANT-Lưu trữ lên đến 8.000 byte dữ liệu thuộc nhiều loại dữ liệu khác nhau, ngoại trừ văn bản, văn bản và dấu thời gian
UNIQUEIDENTIFIER-Lưu trữ số nhận dạng duy nhất trên toàn cầu (GUID)
XML-Lưu trữ dữ liệu định dạng XML. Tối đa 2GB
CURSOR-Lưu trữ một tham chiếu đến một con trỏ được sử dụng cho các hoạt động cơ sở dữ liệu
TABLE-Lưu trữ một tập hợp kết quả để xử lý sau
ok

CREATE PROCEDURE [dbo].[{{nametable}}_create]
{{store.parameter_1}}

AS
    BEGIN
	BEGIN TRY	
	IF(EXISTS
	(
	SELECT 1
	FROM {{nametable}}
	WHERE {{store.required_1}}
	))
	BEGIN
		SELECT 'MESSAGE.employee_code_exist';
	END;
	ELSE
		BEGIN
		INSERT INTO {{nametable}}
		(
		{{store.insert_1}}
		)
		VALUES
		(
		{{store.insert_2}}
		);
		END;
	END TRY

	-- If any error
	BEGIN CATCH
		SELECT 'MESSAGE. failed';
	END CATCH
    END;
	GO

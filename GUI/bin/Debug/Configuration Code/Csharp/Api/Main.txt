using Microsoft.AspNetCore.Authorization;
using Microsoft.AspNetCore.Mvc;
using Model.Model;
using Newtonsoft.Json;
using Service.Admin.Service.Interface;
using ShopVT.Extensions;
using ShopVT.Model;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace {{namespace}}
{
    [Route("api/[controller]")]
   [Authorize]
    [ApiController]
    public class {{nametable}}Controller : BaseController
    {
        private readonly {{iclass.service}} {{variable.service}};

        public {{nametable}}Controller({{iclass.service}} {{nametable}})
        {
            {{variable.service}}={{nametable}};
        }
                {{insert}}
	            {{update}}
	            {{delete}}
                {{getAll}}
                {{search}}
{{dropdown}}
                {{saveFromList}}
                {{getById}}
    }
}











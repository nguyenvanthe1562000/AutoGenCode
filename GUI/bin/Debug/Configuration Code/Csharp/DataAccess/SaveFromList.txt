        /// <summary>
        /// Save information from a list
        /// </summary>
        /// <param name="listjson_employee">List Json contains information of Employee</param>
        /// <param name="updated_by">Information user update</param>
        /// <returns></returns>
        public bool SavefromList({{parameters.init}})
        {
            string msgError = "";
            try
            {
                
                var dt = _dbHelper.ExecuteScalarSProcedureWithTransaction(out msgError, "{{nametable}}_save_from_list",{{parameters.store}});
                if (!string.IsNullOrEmpty(msgError))
                    throw new Exception(msgError);
                return true;
            }
            catch (Exception ex)
            {
                throw ex;
            }
        }

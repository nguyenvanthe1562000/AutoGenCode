
        public async Task<List<{{class.model}}>> Search( {{parameters.init}})
        {
             try
            {
                var dt = await _dbHelper.ExecuteSProcedureReturnDataTableAsync("{{nametable}}_search", {{parameters.store}});
                if (!string.IsNullOrEmpty(dt.message))
                {
                    throw new Exception(dt.message);
                }
                 var list = await dt.Item2.ConvertToAsync<{{class.model}}>();
                return list.ToList();
            }
            catch (Exception ex)
            {
                throw ex;
            }
        }



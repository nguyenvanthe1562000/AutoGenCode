
        public async Task<List<{{class.model}}>>GetAll()
        {
            try
            {
                var dt =await  _dbHelper.ExecuteSProcedureReturnDataTableAsync("{{nametable}}_get_all");
             if (!string.IsNullOrEmpty(dt.message))
                {
                    throw new Exception(dt.message);
                }
                var list = await dt.Item2.ConvertToAsync<{{class.model}}>();
                return list.ToList();
            }
            catch (Exception ex)
            {
                throw ex;
            }
        }

